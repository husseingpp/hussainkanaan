# Secrets — never commit API keys or DB credentials
.env

# Raw API response dumps (regenerated by src/ingest.py)
data/raw/*
!data/raw/.gitkeep

# Archived Excel imports (real business data — keep out of git)
data/imports/

# Python
__pycache__/
*.pyc
.venv/
venv/
.mypy_cache/
.pytest_cache/
