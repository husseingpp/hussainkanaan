# Public assets

Drop static files here. They're served from the site root (`/`).

## Your CV

The "Download CV" button links to `/hussein-kanaan-cv.pdf`.
Add your CV as `public/hussein-kanaan-cv.pdf` to enable it
(or change the path in `data/portfolio.ts` → `personal.cv`).
