# Cloud mode (Phase 3). Copy to `.env` and fill in to run the web app against Supabase.
# When these are set (and not running under Tauri), the app uses the Supabase backend;
# otherwise it falls back to local SQLite (desktop) or seeded sql.js (dev).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
